workflows:
  android-build:
    name: Android Build

    environment:
      node: 20

    scripts:
      - npm install
      - npm run build
      - npx cap sync android
      - cd android
      - ./gradlew assembleDebug

    artifacts:
      - android/app/build/outputs/**/*.apk
